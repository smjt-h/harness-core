version: v1
build:
  excludes:
    - 867-polling-contracts/src/main/proto
breaking:
  use:
    - PACKAGE
lint:
  use:
    - DEFAULT
  except:
    - ENUM_VALUE_PREFIX
    - PACKAGE_VERSION_SUFFIX
    - PACKAGE_DIRECTORY_MATCH

